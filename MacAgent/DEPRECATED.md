# Deprecated packaging

Day-to-day MacAgent UI is the Xcode project:

`../MacAgentApp/MacAgent.xcodeproj`

This folder (`MacAgent/` + `build.sh`) is the older `swiftc` multi-tab Dock app,
kept only as reference. Prefer the transparent overlay agent.
